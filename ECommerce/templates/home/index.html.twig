{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}

    <link href="{{ asset('css/Home.css') }}" rel="stylesheet"/>
    <link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">

    <div class="structure">
        <div class="body-pub">
            <div class="publicité">
                <img class="mySlides" src="{{ asset('Pub/Pub_01.png') }}" style="width: 100%;height: 100%;">
                <img class="mySlides" src="{{ asset('Pub/Pub_02.png') }}" style="width: 100%;height: 100%;">
                <img class="mySlides" src="{{ asset('Pub/Pub_03.png') }}" style="width: 100%;height: 100%;">
                <div style="position: absolute;bottom: 16%;left: 50%;">
                    <span class="w3-badge demo w3-border w3-transparent w3-hover-white" onclick="currentDiv(1)"></span>
                    <span class="w3-badge demo w3-border w3-transparent w3-hover-white" onclick="currentDiv(2)"></span>
                    <span class="w3-badge demo w3-border w3-transparent w3-hover-white" onclick="currentDiv(3)"></span>
                </div>
            </div>
            <div class="promotions-home">
            </div>
        </div>

        <div class="body-promotions">
            <p>Promotions</p>
            <div class="promotions-home">
                <div class="horizontal-big"></div>
                <div class="trait-horizontal"></div>
            </div>
            <div class="promotion-produit">
                <div class="produit-home-promotion">
                    {% for article in articles %}
                        {% if article.id is same as(4) %}
                            {% for image in article.images %}
                                {% if loop.first %}
                                    <img src="{{ image.lien }}" class="img-100"
                                         onclick="window.location.href='article/4'"/>
                                {% endif %}
                            {% endfor %}
                            <p onclick="window.location.href='article/4'">{{ article.nom }}</p>
                            <a> {{ article.prixUnitaire }} &#8364</a>
                        {% endif %}
                    {% endfor %}
                </div>
                <div class="produit-home-promotion">
                    {% for article in articles %}
                        {% if article.id is same as(5) %}
                            {% for image in article.images %}
                                {% if loop.first %}
                                    <img src="{{ image.lien }}" class="img-100"
                                         onclick="window.location.href='article/5'"/>
                                {% endif %}
                            {% endfor %}
                            <p onclick="window.location.href='article/5'">{{ article.nom }}</p>
                            <a> {{ article.prixUnitaire }} &#8364</a>
                        {% endif %}
                    {% endfor %}
                </div>
                <div class="produit-home-promotion">
                    {% for article in articles %}
                        {% if article.id is same as(6) %}
                            {% for image in article.images %}
                                {% if loop.first %}
                                    <img src="{{ image.lien }}" class="img-100"
                                         onclick="window.location.href='article/6'"/>
                                {% endif %}
                            {% endfor %}
                            <p onclick="window.location.href='article/6'">{{ article.nom }}</p>
                            <a> {{ article.prixUnitaire }} &#8364</a>
                        {% endif %}
                    {% endfor %}
                </div>
                <div class="produit-home-promotion">
                    {% for article in articles %}
                        {% if article.id is same as(7) %}
                            {% for image in article.images %}
                                {% if loop.first %}
                                    <img src="{{ image.lien }}" class="img-100"
                                         onclick="window.location.href='article/7'"/>
                                {% endif %}
                            {% endfor %}
                            <p onclick="window.location.href='article/7'">{{ article.nom }}</p>
                            <a> {{ article.prixUnitaire }} &#8364</a>
                        {% endif %}
                    {% endfor %}
                </div>
                <div class="produit-home-promotion">
                    {% for article in articles %}
                        {% if article.id is same as(8) %}
                            {% for image in article.images %}
                                {% if loop.first %}
                                    <img src="{{ image.lien }}" class="img-100"
                                         onclick="window.location.href='article/8'"/>
                                {% endif %}
                            {% endfor %}
                            <p onclick="window.location.href='article/8'">{{ article.nom }}</p>
                            <a> {{ article.prixUnitaire }} &#8364</a>
                        {% endif %}
                    {% endfor %}
                </div>
            </div>
            <div class="voir-plus">
                <img src="{{ asset('icon/cross_01.png') }}" onclick="window.location.href='article/promotion'">
            </div>
        </div>

        <div class="body-promotions">
            <p>Nouveautés</p>
            <div class="promotions-home">
                <div class="horizontal-big"></div>
                <div class="trait-horizontal"></div>
            </div>
            <div class="promotion-produit">
                <div class="produit-home-promotion">
                    {% for article in articles %}
                        {% if article.id is same as(4) %}
                            {% for image in article.images %}
                                {% if loop.first %}
                                    <img src="{{ image.lien }}" class="img-100"
                                         onclick="window.location.href='article/4'"/>
                                {% endif %}
                            {% endfor %}
                            <p onclick="window.location.href='article/4'">{{ article.nom }}</p>
                            <a> {{ article.prixUnitaire }} &#8364</a>
                        {% endif %}
                    {% endfor %}
                </div>
                <div class="produit-home-promotion">
                    {% for article in articles %}
                        {% if article.id is same as(5) %}
                            {% for image in article.images %}
                                {% if loop.first %}
                                    <img src="{{ image.lien }}" class="img-100"
                                         onclick="window.location.href='article/5'"/>
                                {% endif %}
                            {% endfor %}
                            <p onclick="window.location.href='article/5'">{{ article.nom }}</p>
                            <a> {{ article.prixUnitaire }} &#8364</a>
                        {% endif %}
                    {% endfor %}
                </div>
                <div class="produit-home-promotion">
                    {% for article in articles %}
                        {% if article.id is same as(6) %}
                            {% for image in article.images %}
                                {% if loop.first %}
                                    <img src="{{ image.lien }}" class="img-100"
                                         onclick="window.location.href='article/6'"/>
                                {% endif %}
                            {% endfor %}
                            <p onclick="window.location.href='article/6'">{{ article.nom }}</p>
                            <a> {{ article.prixUnitaire }} &#8364</a>
                        {% endif %}
                    {% endfor %}
                </div>
                <div class="produit-home-promotion">
                    {% for article in articles %}
                        {% if article.id is same as(7) %}
                            {% for image in article.images %}
                                {% if loop.first %}
                                    <img src="{{ image.lien }}" class="img-100"
                                         onclick="window.location.href='article/7'"/>
                                {% endif %}
                            {% endfor %}
                            <p onclick="window.location.href='article/7'">{{ article.nom }}</p>
                            <a> {{ article.prixUnitaire }} &#8364</a>
                        {% endif %}
                    {% endfor %}
                </div>
                <div class="produit-home-promotion">
                    {% for article in articles %}
                        {% if article.id is same as(8) %}
                            {% for image in article.images %}
                                {% if loop.first %}
                                    <img src="{{ image.lien }}" class="img-100"
                                         onclick="window.location.href='article/8'"/>
                                {% endif %}
                            {% endfor %}
                            <p onclick="window.location.href='article/8'">{{ article.nom }}</p>
                            <a> {{ article.prixUnitaire }} &#8364</a>
                        {% endif %}
                    {% endfor %}
                </div>
            </div>
            <div class="voir-plus">
                <img src="{{ asset('icon/cross_01.png') }}" onclick="window.location.href='article/promotion'">
            </div>
        </div>

    </div>


    <script>
        var slideIndex = 1;
        showDivs(slideIndex);

        function plusDivs(n) {
            showDivs(slideIndex += n);
        }

        function currentDiv(n) {
            showDivs(slideIndex = n);
        }

        function showDivs(n) {
            var i;
            var x = document.getElementsByClassName("mySlides");
            var dots = document.getElementsByClassName("demo");
            if (n > x.length) {
                slideIndex = 1
            }
            if (n < 1) {
                slideIndex = x.length
            }
            for (i = 0; i < x.length; i++) {
                x[i].style.display = "none";
            }
            for (i = 0; i < dots.length; i++) {
                dots[i].className = dots[i].className.replace(" w3-white", "");
            }
            x[slideIndex - 1].style.display = "block";
            dots[slideIndex - 1].className += " w3-white";
        }
    </script>
{% endblock %}
